
        #include <bits/stdc++.h>
             
        using namespace std;
         
        typedef long long ll; 
        const int N = 1e5+5;
        #define all(x) x.begin(),x.end()
        #define MOD 1000000007
        #define pii pair<int,int>
        #define pis pair<int,string>
        #define F first
        #define S second
        #define LCM(a,b) ((a*b)/__gcd(a,b))
        #define inf 1e15
        #define test ll cse;cin>>cse;for(ll i=1;i<=cse;i++)
        #define fast ios_base::sync_with_stdio(0);cin.tie(NULL);cout.tie(NULL);
        #define PI 3.14159265
        #define MAXN 10000001

ll query(ll x,ll y){
    printf("Q %lld %lld",x,y);  fflush(stdout);
    ll ans; scanf("%lld", &ans);

    return ans;
}

int main(){
    
    ll t; scanf("%lld",&t); while(t-- > 0){


        ll A,B,C,D,E,F;
        A = query(0,0);
        B = query(1000000000,0);
        C = query(0,1000000000);
        D = query(1000000000,1000000000);
        E = query()
    }
    return 0;
}